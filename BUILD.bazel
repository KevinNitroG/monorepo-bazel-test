load("@npm//:defs.bzl", "npm_link_all_packages")
load("@gazelle//:def.bzl", "gazelle")

npm_link_all_packages(
    name = "node_modules"
)

# gazelle:prefix github.com/KevinNitroG/monorepo-bazel-test
gazelle(name = "gazelle")
